/*!
	Type definition for texts that ought to be localized.

	! Always prefer strong typed usage instead of 'auto'. !
	! This will make searches and parses easier and more reliable. !

	Always prefer the usage of this type instead of 'string' when
	dealing with texts that will be localized (user interface, ...)
*/
typedef string LocalizedString;

/*!
	Type definition for texts that ought to be localized.

	! Always prefer strong typed usage instead of 'auto'. !
	! This will make searches and parses easier and more reliable. !

	Always prefer the usage of this type instead of 'string' when
	dealing with texts that will be localized (user interface, ...)
*/
class LocalizedString : string 
{
};