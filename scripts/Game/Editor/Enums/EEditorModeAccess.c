enum EEditorModeAccess
{
	BASE, //< Base modes defined in config, can be adjusted by admin
	ROLE, ///< Modes based on player role
	//GAME_MODE, ///< Modes controlled by game mode rules (disabled, prevented ADMIN from removing modes)
};